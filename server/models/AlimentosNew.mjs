import Sequelize from 'sequelize'

      class AlimentosNew extends Sequelize.Model{

        	static init(sequelize,DataTypes){

          		return super.init({
},{sequelize})
}}
export default AlimentosNew